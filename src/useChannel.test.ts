// import { renderHook, act } from "@testing-library/react-hooks";
// import { useChannel } from "./useChannel";

// test("useChannel init", () => {
//   const { result } = renderHook(() => useChannel<string>("test"));
//   expect(result.current.length).toEqual(2); // .latestMessage).toBeDefined;
// });
